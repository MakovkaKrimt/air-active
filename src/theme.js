// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#d92228",
    },
    secondary: {
      main: "#4D74A2",
    },
  },
});

export default theme;
